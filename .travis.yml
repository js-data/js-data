sudo: true
language: node_js
node_js:
- "10"
- "8"
- "6"
- "4"
cache:
  directories:
  - node_modules/
before_install:
  - npm i -g nyc codecov
after_success:
  - nyc report --reporter=lcov > coverage.lcov && codecov
